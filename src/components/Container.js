//Imports REACT
import React from "react";

//Code for the Bootstrap container
const Container = props => (
  <div className="container">
    {props.children}
  </div>
);

//Exports Container
export default Container;
